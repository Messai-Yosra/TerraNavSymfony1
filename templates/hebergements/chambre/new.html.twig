{% extends 'baseClient.html.twig' %}

{% block titre %}Nouvelle Chambre - TerraNav{% endblock %}

{% block content %}
    <div class="container-xxl py-5">
        <div class="container">
            <div class="text-center wow fadeInUp" data-wow-delay="0.1s">
                <h1 class="mb-5">Create new Chambre</h1>
            </div>

            <div class="row g-4 justify-content-center">
                <div class="col-lg-8 col-md-12 wow fadeInUp" data-wow-delay="0.3s">
                    <div class="bg-light rounded p-4">
                        {{ include('hebergements/chambre/_form.html.twig') }}

                        <div class="text-center mt-4">
                            <a href="{{ path('app_chambre_index') }}" class="btn btn-primary">
                                <i class="fas fa-arrow-left me-2"></i> Back to list
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}