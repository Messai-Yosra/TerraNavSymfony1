{% extends "baseClient.html.twig" %}

{% block title %}Modifier l'Offre - {{ offre.titre }}{% endblock %}

{% block content %}
    <!-- Page Header Start -->
    <div class="container-fluid page-header mb-5 p-0" style="background-image: url({{ asset('img/voyages/1.avif') }});">
        <div class="container-fluid page-header-inner py-5">
            <div class="container text-center pb-5">
                <h1 class="display-3 text-white mb-3 animated slideInDown">Modifier l'offre</h1>
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb justify-content-center text-uppercase">
                        <li class="breadcrumb-item"><a href="{{ path('app_offres_agence') }}">Vos offres</a></li>
                        <li class="breadcrumb-item text-white active" aria-current="page">Modification</li>
                    </ol>
                </nav>
            </div>
        </div>
    </div>
    <!-- Page Header End -->

    <!-- Offre Form Start -->
    <div class="container-xxl py-5">
        <div class="container">
            <div class="text-center wow fadeInUp" data-wow-delay="0.1s">
                <h6 class="section-title text-center text-primary text-uppercase">Formulaire de modification</h6>
                <h1 class="mb-5">Modifier <span class="text-primary text-uppercase">{{ offre.titre }}</span></h1>
            </div>
            <form id="offreForm" action="{{ path('app_modifier_offre_bd', {'id': offre.id}) }}" method="POST" enctype="multipart/form-data">
                <div class="row g-5">
                    <div class="col-lg-6">
                        <!-- Affichage de l'image existante -->
                        <div class="row g-3 mb-4">
                            <div class="col-12 text-center">
                                <img class="img-fluid rounded wow zoomIn hover-zoom"
                                     src="{{ asset(offre.transformSingleImagePath(offre.imagePath)) }}"
                                     style="max-width: 80%; margin-top: 10%;"
                                     onerror="this.src='{{ asset('img/about-1.jpg') }}'">
                            </div>
                        </div>
                    </div>

                    <div class="col-lg-6">
                        <div class="wow fadeInUp" data-wow-delay="0.2s">
                            <!-- Section Informations de base -->
                            <div class="card mb-4 border-primary hover-effect">
                                <div class="card-header bg-primary text-white">
                                    <i class="fas fa-info-circle me-2"></i>Informations de l'offre
                                </div>
                                <div class="card-body">
                                    <div class="row g-3">
                                        <!-- Titre -->
                                        <div class="col-12">
                                            <div class="form-floating">
                                                <input type="text" class="form-control" id="titre" name="titre"
                                                       placeholder="Titre de l'offre" value="{{ offre.titre }}" required>
                                                <label for="titre"><i class="fas fa-heading me-2 text-primary"></i>Titre de l'offre</label>
                                            </div>
                                        </div>

                                        <!-- Réduction -->
                                        <div class="col-md-6">
                                            <div class="form-floating">
                                                <input type="number" class="form-control" id="reduction" name="reduction"
                                                       min="1" max="100" step="1" placeholder="Réduction"
                                                       value="{{ offre.reduction }}" required>
                                                <label for="reduction"><i class="fas fa-percent me-2 text-primary"></i>Réduction (%)</label>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- Section Période de validité -->
                            <div class="card mb-4 border-primary hover-effect">
                                <div class="card-header bg-primary text-white">
                                    <i class="far fa-calendar-alt me-2"></i>Période de validité
                                </div>
                                <div class="card-body">
                                    <div class="row g-3">
                                        <!-- Date de début -->
                                        <div class="col-md-6">
                                            <div class="form-floating">
                                                <input type="datetime-local" class="form-control" id="dateDebut"
                                                       name="dateDebut" value="{{ offre.dateDebut|date('Y-m-d\\TH:i') }}" required>
                                                <label for="dateDebut"><i class="fas fa-hourglass-start me-2 text-primary"></i>Date de début</label>
                                            </div>
                                        </div>

                                        <!-- Date de fin -->
                                        <div class="col-md-6">
                                            <div class="form-floating">
                                                <input type="datetime-local" class="form-control" id="dateFin"
                                                       name="dateFin" value="{{ offre.dateFin|date('Y-m-d\\TH:i') }}" required>
                                                <label for="dateFin"><i class="fas fa-hourglass-end me-2 text-primary"></i>Date de fin</label>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- Section Description -->
                            <div class="card mb-4 border-primary hover-effect">
                                <div class="card-header bg-primary text-white">
                                    <i class="fas fa-align-left me-2"></i>Description
                                </div>
                                <div class="card-body">
                                    <!-- Image -->
                                    <div class="col-md-12">
                                        <div class="form-group">
                                            <label for="image" class="form-label"><i class="fas fa-image me-2 text-primary"></i>Modifier l'image</label>
                                            <input type="file" class="form-control hover-file-input" id="image"
                                                   name="image" accept="image/*">
                                            <small class="form-text text-muted">Sélectionnez une nouvelle image pour remplacer l'actuelle</small>
                                            <div id="imagePreview" class="mt-2">
                                                <input type="hidden" name="existing_image" value="{{ offre.imagePath }}">
                                            </div>
                                        </div>
                                    </div>
                                    <div class="form-floating mt-3">
                                        <textarea class="form-control" placeholder="Description détaillée"
                                                  id="description" name="description" style="height: 100px">{{ offre.description }}</textarea>
                                        <label for="description"><i class="fas fa-info-circle me-2 text-primary"></i>Description détaillée</label>
                                    </div>
                                </div>
                            </div>

                            <!-- Boutons -->
                            <div class="row g-3 mt-2">
                                <div class="col-6">
                                    <button class="btn btn-primary w-100 py-3 btn-hover" type="submit">
                                        <i class="fas fa-save me-2"></i>Enregistrer
                                    </button>
                                </div>
                                <div class="col-6">
                                    <a href="{{ path('app_offres_agence') }}" class="btn btn-secondary w-100 py-3 btn-hover" id="cancelBtn">
                                        <i class="fas fa-times-circle me-2"></i>Annuler
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>
    <!-- Offre Form End -->

    <style>
        /* Effets de hover */
        .hover-effect {
            transition: all 0.3s ease;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        .hover-effect:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
        }

        .hover-zoom {
            transition: transform 0.3s ease;
        }

        .hover-zoom:hover {
            transform: scale(1.05);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }

        .btn-hover {
            transition: all 0.3s ease;
        }

        .btn-hover:hover {
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }

        /* Style pour les cartes */
        .card-header {
            font-weight: 600;
        }

        /* Style pour les inputs file */
        .hover-file-input:hover {
            border-color: #0d6efd;
            background-color: #f8f9fa;
        }

        /* Style pour les labels avec icônes */
        .form-floating label i {
            width: 20px;
            text-align: center;
        }

        /* Animation pour les champs */
        .form-control {
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .form-control:focus {
            box-shadow: 0 0 0 0.25rem rgba(13, 110, 253, 0.25);
        }

        /* Ajustement des marges */
        .col-lg-6:first-child {
            padding-left: 2rem;
        }

        .col-lg-6:last-child {
            padding-right: 2rem;
        }

        /* Preview image */
        #imagePreview img {
            max-width: 100px;
            max-height: 100px;
            margin-right: 10px;
            margin-bottom: 10px;
            border: 1px solid #ddd;
            border-radius: 4px;
            padding: 5px;
        }
    </style>

    <script>
        document.addEventListener('DOMContentLoaded', function () {
            // Gestion de la soumission du formulaire
            document.getElementById('offreForm').addEventListener('submit', function (e) {
                // Validation des dates
                const dateDebut = new Date(document.getElementById('dateDebut').value);
                const dateFin = new Date(document.getElementById('dateFin').value);

                if (dateFin <= dateDebut) {
                    alert('La date de fin doit être postérieure à la date de début');
                    e.preventDefault();
                    return;
                }

                // Validation de la réduction
                const reduction = parseInt(document.getElementById('reduction').value);
                if (reduction <= 0 || reduction > 100) {
                    alert('La réduction doit être comprise entre 1% et 100%');
                    e.preventDefault();
                    return;
                }
            });

            // Gestion du bouton Annuler
            document.getElementById('cancelBtn').addEventListener('click', function (e) {
                e.preventDefault();
                if (confirm('Êtes-vous sûr de vouloir annuler les modifications ?')) {
                    window.location.href = this.href;
                }
            });

            // Animation au survol des champs
            const formControls = document.querySelectorAll('.form-control, .form-select');
            formControls.forEach(control => {
                control.addEventListener('mouseenter', function () {
                    this.style.borderColor = '#0d6efd';
                });
                control.addEventListener('mouseleave', function () {
                    this.style.borderColor = '#ced4da';
                });
            });

            // Preview de l'image
            document.getElementById('image').addEventListener('change', function(e) {
                const preview = document.getElementById('imagePreview');
                preview.innerHTML = '';

                // Conserver le champ hidden avec l'image existante
                const hiddenInput = document.createElement('input');
                hiddenInput.type = 'hidden';
                hiddenInput.name = 'existing_image';
                hiddenInput.value = '{{ offre.imagePath }}';
                preview.appendChild(hiddenInput);

                if (this.files && this.files[0]) {
                    const reader = new FileReader();
                    reader.onload = function(e) {
                        const img = document.createElement('img');
                        img.src = e.target.result;
                        img.className = 'img-thumbnail';
                        preview.appendChild(img);
                    }
                    reader.readAsDataURL(this.files[0]);
                }
            });
        });
    </script>
{% endblock %}